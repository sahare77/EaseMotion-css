# CSS-only Multi-step Form Tracker

A responsive progress tracking bar using CSS counters.

## Strategy
- `counter-reset` and `counter-increment` automatically number the steps.
- Absolute positioning of a background line gives the illusion of a connected path.
- Extensible and very easy to integrate with JS frameworks.
